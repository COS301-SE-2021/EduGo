import 'package:flutter/material.dart';

class DetectMarkerPage extends StatelessWidget {
  const DetectMarkerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
